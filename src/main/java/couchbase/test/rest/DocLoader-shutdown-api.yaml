openapi: "3.0.3"
info:
  title: "DocLoader API"
  description: "DocLoader API"
  version: "1.0.0"
servers:
  - url: "https://DocLoader"
paths:
  /shutdown-app:
    get:
      summary: "GET shutdown-app"
      operationId: "shutdownApp"
      responses:
        "200":
          description: "OK"